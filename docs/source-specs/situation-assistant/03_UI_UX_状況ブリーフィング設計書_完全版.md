# UI/UX 設計書 — 状況ブリーフィング（完全版）

Last updated: 2026-05-25

---

## 1. 設計原則

1. **ノーサプライズ**: 既存 BottomMonitor の操作パターンを踏襲する。新しい UI パターンを導入しない。
2. **mock ファースト**: 実 AI 未接続でも動作する。mock バナーで状態を明示する。
3. **情報密度の制御**: 4D ブリーフィングは折りたたみ可能。デフォルトは展開表示。
4. **操作最小化**: ボタン 1 つで生成。設定は最小限。

---

## 2. ブリーフィングタブ レイアウト

```
┌─────────────────────────────────────────────────┐
│ ログ | メトリクス | ... | ストレージ | Roadmap | 状況 │  ← タブ追加
├─────────────────────────────────────────────────┤
│  [ブリーフィングを生成]  入力: [全ログ ▼]          │
├─────────────────────────────────────────────────┤
│  📋 ブリーフィング  ● error  2026-05-25 02:17    │
│                                                   │
│  ## What                                          │
│  12 ノードが実行されました。...                    │
│                                                   │
│  ## Why                                           │
│  チェックノードでエラーが発生...                   │
│                                                   │
│  ## How                                           │
│  再試行候補が 1 件あります...                      │
│                                                   │
│  ## Next                                          │
│  1. 再試行ボタンでチェックノードを再実行する        │
│                                                   │
│  ⚠ mock モード（実 API 未接続）                   │
└─────────────────────────────────────────────────┘
```

---

## 3. UI コンポーネント仕様

### 3.1 生成コントロール

| 要素 | 仕様 |
|---|---|
| 生成ボタン | `primary-button`、テキスト: 「ブリーフィングを生成」 |
| 生成中 | スピナー + 「生成中...」+ キャンセルボタン |
| 入力ソース選択 | `<select>`: 全ログ / 最新 Run のみ / エラーのみ |

### 3.2 ブリーフィング表示カード

| 要素 | 仕様 |
|---|---|
| ヘッダー | 「📋 ブリーフィング」+ 重要度バッジ + 生成日時 |
| 本文 | 4D セクション（What / Why / How / Next）、`<pre>` or マークダウンレンダリング |
| 折りたたみ | セクション単位で折りたたみ可能（将来機能）。MVP は全展開固定 |
| mock バナー | 実 API 未接続時: `icon-button` スタイルの警告バナー |

### 3.3 重要度バッジ

| 値 | 表示色 | CSS クラス例 |
|---|---|---|
| `info` | グレー / 青 | `.badge-info` |
| `warn` | オレンジ | `.badge-warn` |
| `error` | 赤 | `.badge-error` |

---

## 4. インタラクション仕様

### 4.1 生成ボタン押下

```
[生成ボタン] →
  status = 'generating'
  ボタン disabled + スピナー表示
  →（完了）status = 'done', ブリーフィング表示
  →（失敗）status = 'error', エラーバナー表示
```

### 4.2 再生成

- 生成完了後もボタンは有効。再押下で最新状態から再生成する。
- 前回のブリーフィングは新しいものに上書きされる。

### 4.3 空状態（初期表示）

```
ブリーフィングはまだ生成されていません。
「ブリーフィングを生成」を押してください。

ヒント: Run を実行した後に生成すると、実行状態の説明が得られます。
```

---

## 5. アクセシビリティ

- 生成ボタン: `aria-label="状況ブリーフィングを生成"`
- スピナー: `aria-label="生成中"` + `role="status"`
- 重要度バッジ: `aria-label="重要度: {severity}"`
- エラーバナー: `role="alert"`

---

## 6. 既存 HUD との関係

本体仕様 `03_UI_UX_認知HUD設計書_完全版.md` で定義された認知 HUD（Cognitive HUD）との関係:

- 状況ブリーフィングタブは、認知 HUD の **言語化レイヤー** として機能する。
- HUD Signal（実行状態のビジュアル表示）の内容を自然言語で補完する。
- HUD Signal の表示そのものは変更しない。

---

## 7. モバイル / レスポンシブ

MVP では BottomMonitor の既存レスポンシブ挙動を踏襲する。専用対応は不要。

---

## 8. 将来拡張（MVP スコープ外）

| 機能 | 優先度 | 理由 |
|---|---|---|
| セクション折りたたみ | 低 | MVP では全展開で十分 |
| 音声読み上げ（TTS） | 低 | ブラウザ API で実現可能だが混乱リスクあり |
| 動画・アバター | 最低 | 別フェーズ |
| ブリーフィング履歴保存 | 中 | durable run history と連動 |
| カスタムプロンプト入力 | 中 | 上級ユーザー向け |
