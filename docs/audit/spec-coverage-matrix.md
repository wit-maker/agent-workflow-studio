# Spec Coverage Matrix

Last updated: 2026-06-03

Status values:

- Done: MVP behavior substantially exists.
- Partial: useful MVP behavior exists, but full spec is not complete.
- Missing: no meaningful implementation yet.
- Mismatch: implementation shape differs from the source-spec model and needs reconciliation.
- Risk: current implementation may block or distort later phases if not handled.

> **Concept layer note (Issue #31)**: For the cognitive HUD and the Situation Narration Layer, the current MVP exposes summary surfaces (`認知HUD` tab, `ブリーフィング` tab, `実行詳細` tab). These surfaces are **provisional MVP display panels**, not the full layers. The "Spec area" rows below are intentionally evaluated against the full source-spec layer, not against the MVP surface. See `docs/project/concept-layer-correction.md` and `current-implementation-map.md` for the concept-layer split.

## UI Coverage

| Spec area | Status | Current coverage | Gap / risk |
|---|---|---|---|
| UI-01 Main canvas | Partial | React Flow is now the default fresh canvas and shows 12 nodes, 13 edges, status, ports, validation, inline previews, minimap HUD, zoom-mode classes, workflow group layer, measured anchored canvas-overlay HUDs, selected path dimming/highlight, semantic focus paths from current runtime attention state, selected-node audit evidence summaries, runtime edge state/health classes derived from execution graph + run trace, on-demand HUD notification/history/density overlay, and Run Detail deep links back to node/edge selection. Standard canvas remains as fallback. | Full editing, auto layout, DnD node creation, complete node deletion UX, persisted HUD preferences, animated replay-backed focus, edge-level durable replay, and complete React Flow migration are incomplete. |
| UI-02 Parts library | Partial | Searchable MVP part list shows categories, descriptions, and I/O. | Agent role/data type filters and reliable add-from-library workflow need hardening. |
| UI-03 Input / collection | Partial | Text/file input nodes exist as sample nodes. | Input source management, acquisition settings, normalization status, and previews are not first-class screens. |
| UI-04 Processing / control flow | Partial | Route, check, retry/error/review paths exist in mock execution. Optional connection `runtimePolicy` metadata can now be edited for condition / delay / retry / error-route safe summaries, and fixed policy presets can be evaluated into safe runtime audit metadata. | Branch/parallel design, full runtime policy enforcement, arbitrary expression evaluation, and visual control-flow authoring are incomplete. |
| UI-05 Artifact stage | Partial | Preview/Markdown/JSON-like artifact display, evaluation, review, and version panels exist. | Diff, publish preparation, durable approval, and artifact persistence are incomplete. |
| UI-06 Block editing | Partial | Inspector edits title, description, role, config JSON, ports, connections, and optional connection runtime policy metadata. | Prompt, tools, security, test run, last-run detail, rich policy diagnostics, and settings history are missing. |
| UI-07 External connection / AI integration | Partial | Agent tab shows mock connectors and credential boundary. | Real adapter connection state, test connection, credential references, and read/write approval modes are missing. |
| UI-08 Observation / execution monitor | Partial | Logs, metrics, queue, output, execution graph, evaluation, agent, storage tabs, safe runtime route events, Run Detail evidence, run selection between current trace and safe `traceAudit` snapshots, node/edge deep links, two-run safe audit metadata comparison including runtime event counts, step-level safe evidence grouping, focused node/edge scoped diff, focused edge safe route-event metadata diff, and safe connection policy summaries exist. | Animated replay, visual run comparison animation, error rate, parallelism, resource load, enforced edge policy, and full edge-level route reconstruction are missing. |
| UI-09 Evaluation / rebuild | Partial | PASS/REVIEW/FAIL mock flow, local evaluation, human review, rebuild requests, and artifact versions exist. | Diff-based review, persistent human decisions, and improvement prompt accumulation are missing. |
| UI-10 Reinforced reuse state | Partial | Template save/load/search/duplicate/preview exists with metadata. | Recipes, knowledge, success/failure pattern assets, sharing, and version editing are missing. |
| UI-11 Cognitive HUD settings | Partial | No dedicated HUD settings screen. The cognitive HUD now has receiver surfaces — `CognitiveHudOverlay` (canvas), `HudNotificationBundle` (notification/history/density overlay), anchored `SelectedObjectHud`, anchored `SelectedEdgeHud`, right panel `Situation` mode, `CanvasCommandHud`, minimap HUD, semantic focus path, selected path dimming/highlight, and `CriticalOverlay` (root) — plus node-level HUD badges for failed/review/blocked/retry and semantic `ATTN` / `PATH` badges. Session-only `quiet / balanced / deep` density, danger visibility labels, and collapse policy are modeled as derived HUD view state. | Persisted HUD priority settings, durable notification settings/read state, always-on edge badges beyond semantic focus, critical short audio cue, and complete user-tunable depth policy are incomplete. |
| UI-13 Cognitive Workspace Shell (Issue #34) | Partial | `GameHudShell` now turns the workspace into a Canvas First layout. Palette, detail, and BottomMonitor surfaces are HUD drawer/console overlays. `CanvasCommandHud` provides minimal always-on controls, semantic attention source chips, trace/audit evidence source chips, notification toggle, density toggle, Validate action, and Run Detail trace action. `HudNotificationBundle` provides on-demand HUD feed, run history summary, session-only density settings, and Replay links. Selected nodes and edges show measured anchored floating HUDs that avoid DOM-measured HUD surfaces, including notification overlay collision. MiniMap, zoom-mode representation, inline previews, static workflow groups, selected node/edge focus dimming/highlight, semantic runtime focus, rich central HUD variants, selected-node audit evidence summaries, runtime-backed selected-edge summaries, and Run Detail deep links are implemented. | Full per-region content (Workflow Library, Templates), removal of duplicate BottomMonitor tabs, persisted HUD preferences, edge-level replayable audit backing, durable notification state, and real assistant audio/avatar/video renderers are not implemented. |
| UI-12 Run history / audit log | Partial | Local workflow history, transient logs, durable run record list, credential-safe `traceAudit` snapshots and `traceAudit.runtimeEvents` inside existing run history records, Run Detail run selection, safe audit snapshot recall, node/edge deep links, metadata diff, runtime-event count diff, focused edge safe route-event metadata diff, step-level safe evidence grouping, focused scoped diff between two audit snapshots, and safe connection policy summaries exist. | Animated replay, reproduction, export, visual comparison animation, enforced edge policy, and full edge-level route reconstruction are not complete. |
| UI-13 Situation Narration Layer (added) | Partial | 4D mock text briefing exists as the **minimum output channel** (`BriefingPanel`). | Timeline Narration, Incident Replay, Avatar Briefing, Audio Alert Briefing, Visual Explanation Render, Workflow News Video, Briefing Video Composer, Next Action Briefing standalone, role switch (concierge / secretary / narrator / report-relay / situation strategist) are missing. Voice / avatar / video are MVP-out but must not be removed from spec. |

## Functional Coverage

| Area | Status | Current coverage | Gap / risk |
|---|---|---|---|
| Node categories | Mismatch | MVP has categories such as 開始, 入力, 変換, 制御, 実行, 接続, 品質, 回収, 出力, 記録, テンプレート. | Full source categories include 起点, 入力取得, 整形・前処理, 分岐・ルーティング, 検査, 集約, 観測, 改善, 認知HUD, 安全・権限. Mapping needs normalization before scaling parts. |
| Data types | Partial | `WorkflowDataType` covers many content, control, artifact, metric, error, command, and review types. | CredentialRef must remain outside normal data flow; richer document/source/trace types may be needed. |
| Edge types | Partial | `ConnectionKind` covers data, instruction, result, decision, evidence, log, error, retry, approval, resource, template, and improvement. `WorkflowConnection` can optionally hold safe `runtimePolicy` metadata for condition / delay / retry / error-route summaries. Selected edge HUD and React Flow edges derive runtime state/health from `ExecutionGraph`, `RunTrace`, connection metrics, validation, and optional policy metadata. | Runtime enforcement, expression evaluation, durable edge audit events, and replay across historical runs are not complete. |
| Run modes | Partial | Run All, Run Selected, Run From Selected, Dry Run, and mock-only Validate exist. | Mock Run as explicit named mode, full Stop, Retry as full run mode, Error Route, Resume, and Replay are incomplete. |
| Metrics / trace / audit log | Partial | Tokens, cost, latency, success rate, queue count, retry count, bottleneck, logs, execution graph, read-only run trace evidence, safe runtime route events, semantic focus projection, durable safe `traceAudit` snapshots with normalized `runtimeEvents`, Run Detail run selection, node/edge deep links, safe metadata run diff, runtime-event count diff, step-level safe evidence grouping, focused scoped diff, and safe connection policy summaries exist. | Replayable audit log, visual run diff animation, durable connector queue, edge-level replay, enforced policy events, and policy-backed regression analysis are incomplete. |
| Cognitive HUD | Partial | `HudSignal` / `HudSnapshot` / `HudCounts` derive priority, summary, focus, zoom HUD, inline preview, workflow group, selected-node HUD, selected-edge HUD, semantic focus path, central HUD variants, runtime edge semantics, HUD notification bundle, HUD history entries, session density policy, and Run Detail replay view state. Node status badges, measured selected-node object HUD, measured selected-edge flow HUD, semantic attention badges, selected/semantic path dimming/highlight, minimap HUD, zoom mode, inline previews, bottleneck/failure/approval/validation indicators, selected-node durable audit evidence summaries, selected-edge runtime route/health/evidence/policy summaries, Run Detail focused-edge deep link, notification/history/density overlay, and node/edge deep links are visible. | Cognitive HUD as a complete **attention-allocation layer** spanning Canvas / Inspector / modals is still incomplete. Always-on edge badges beyond runtime class projection, durable notification read/ack/pin, critical short audio cue, animated replay, replayable edge audit backing, and persisted 5-dimension HUD preferences are incomplete. |
| Situation Narration Layer | Partial | 4D mock briefing (`BriefingPanel`) reads HUD snapshot, run trace step evidence, logs, metrics, and connector jobs, then produces text, voice script, avatar script, visual timeline, and human decision prompt. | This is the **minimum mock output channel** only. Real Timeline Narration, Incident Replay, Voice Generator, Avatar Narrator, Visual Highlight Renderer, Briefing Video Composer, role group switch, past→present→future timeline narration, and generated audio/avatar/video assets are missing. Voice / avatar / video must remain in spec even while renderer-out. |
| Safety gates | Partial | Credential boundary, mock connector boundary, confirmations, review-required flow, and docs exist. | Spec check, secret scan placeholder, git guard, publish gate, shell command risk check, loop/cost limits, and durable approval gate are missing. |
| Templates / recipes / knowledge | Partial | Local templates with metadata, preview, duplicate, search, and load confirmation exist. | Recipes, knowledge cards, success/failure pattern capture, versioning, and next-best-action are missing. |
| External adapters | Risk | Mock registry and connector jobs exist. UI states real APIs are not connected. | Real adapter interfaces for Codex, Claude Code, Gemini, Hermes, Grok/X, GitHub, Local Shell, and File System are missing or conceptual. |
| Local-first / Tauri / file storage | Risk | localStorage persistence and `IStorageAdapter` exist; Tauri/file storage docs exist. | App still relies on direct localStorage helpers; file persistence, Tauri filesystem, SQLite/local DB, secure credential store, and migrations are missing. |
